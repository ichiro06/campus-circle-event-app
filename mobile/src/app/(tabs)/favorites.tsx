import { ShellScreen } from "@/components/shell-screen";

export default function FavoritesScreen() {
  return (
    <ShellScreen
      description="お気に入りを見るにはログインが必要です。"
      eyebrow="LOGIN REQUIRED"
      title="お気に入り"
    />
  );
}
